'use strict'

function projects(){
    // Yes No Question 1
    var logo = prompt('Did I design the Next Adventure logo?');
    console.log('Did I design the Next Adventure logo?', logo);

    var p = document.getElementById('logo-response');

    if(logo.trim().toLowerCase() === 'no' === 'No' === 'N') {
        p.textContent = 'I did not design the PCT Days logo, but I wish I could have had a shot at it.';
    }
    else {
        p.textContent = 'No problem, let\'s see what else you might need.';
    }
    //Yes No Question 2
    var letterpress = prompt('Do you need letterpress wedding invitations?');
    console.log('are they looking for letterpress services?', letterpress);

    var p = document.getElementById('letterpress-response');

    if(letterpress.)

}

